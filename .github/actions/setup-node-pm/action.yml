name: Setup Node package manager
description: Install Node or Bun with dependency caching and run a clean install.

inputs:
  package-manager:
    description: npm or bun
    required: true
  node-version-file:
    description: File that pins Node (npm only)
    default: .nvmrc
  bun-version:
    description: Bun version (bun only)
    default: latest

runs:
  using: composite
  steps:
    - name: Setup Node.js
      if: inputs.package-manager == 'npm'
      uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
      with:
        node-version-file: ${{ inputs.node-version-file }}
        cache: npm
    - name: Install (npm)
      if: inputs.package-manager == 'npm'
      shell: bash
      run: npm ci

    - name: Setup Bun
      if: inputs.package-manager == 'bun'
      uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2
      with:
        bun-version: ${{ inputs.bun-version }}
    - name: Cache bun
      if: inputs.package-manager == 'bun'
      uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4
      with:
        path: ~/.bun/install/cache
        key: bun-${{ runner.os }}-${{ hashFiles('**/bun.lock', '**/bun.lockb') }}
        restore-keys: bun-${{ runner.os }}-
    - name: Install (bun)
      if: inputs.package-manager == 'bun'
      shell: bash
      run: bun install --frozen-lockfile
